option_settings:
  # Docker container configuration
  # Note: Docker version is managed by the Elastic Beanstalk platform
  # Removing explicit version specification to use platform defaults
  aws:elasticbeanstalk:container:docker: {}
